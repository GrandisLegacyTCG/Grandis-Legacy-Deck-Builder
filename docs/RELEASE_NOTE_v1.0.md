# Deck Builder v1.0 — Release Note

- Style 1 uses the locked Deck Builder v3.12 and is the default GitHub Pages page.
- Style 2 uses the locked Deck Builder v2.8 under `/style-2/`.
- Added compact Style 1/Style 2 switch buttons beside the Deck Builder title.
- Added **GO TO VS AI** and **GO TO PVP** navigation to both styles.
- Shared the card art, UI assets, and Noto Sans binaries between both styles to avoid duplicate files.
- No deck schema, card definition, starter deck, compatibility, or copy-limit change.
